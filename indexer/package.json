{
  "name": "fund-wave-indexer",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ fund-wave-indexer",
    "create-local": "graph create --node http://localhost:8020/ fund-wave-indexer",
    "remove-local": "graph remove --node http://localhost:8020/ fund-wave-indexer",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 fund-wave-indexer",
    "test": "graph test"
  },
  "devDependencies": {
    "@graphprotocol/graph-cli": "^0.97.0",
    "@graphprotocol/graph-ts": "^0.38.0",
    "matchstick-as": "^0.6.0"
  }
}
